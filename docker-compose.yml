services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: LinkShorter
    ports:
      - "8080:8080"
    environment:
      GIN_MODE: release
    restart: unless-stopped
